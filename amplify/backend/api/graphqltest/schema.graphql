type Todo
  @model
  @auth(
    rules: [
      {allow: owner, ownerField: "owner", operations: [create, delete, read]},
      # {allow: private, operations:[update]}
    ]
  )
{
  id: ID!
  name: String!
  description: String
}
